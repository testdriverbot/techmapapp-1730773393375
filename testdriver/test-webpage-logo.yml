version: 4.0.80
steps:
  - prompt: Assert that the webpage loads successfully.
    commands:
      - command: assert
        expect: Understand the Tech World. is visible on the webpage
  - prompt: >-
      Assert that the TechMap logo is visible on the homepage.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: TechMap logo is visible on the homepage
